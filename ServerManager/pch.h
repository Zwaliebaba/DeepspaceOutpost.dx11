#pragma once

// Precompiled header for the ServerManager app. Pulls in the client engine umbrella
// (NeuronClient.h -> NeuronCore.h), so the engine core, graphics/GUI and networking
// are available to every translation unit.
#include "NeuronClient.h"
